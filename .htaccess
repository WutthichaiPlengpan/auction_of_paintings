<IfModule mod_rewrite.c>
    # 1. ปิดการแสดงผลรายชื่อไฟล์และโฟลเดอร์ (แก้ปัญหา Index of /)
    Options -Indexes

    RewriteEngine On

    # 2. ส่ง Request ทั้งหมด (ที่ไม่ได้เจาะจงโฟลเดอร์ public) เข้าไปที่โฟลเดอร์ public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>